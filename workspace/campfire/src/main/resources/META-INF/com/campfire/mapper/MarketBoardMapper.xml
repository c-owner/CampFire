<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campfire.mapper.MarketBoardMapper">
	<!-- 검색 -->
	<sql id="criteria">
		<if test="cri.type != null and cri.keyword != null">
			<trim prefix="(" suffix=") AND" prefixOverrides="OR">
				<foreach item="cri.type" collection="typeList">
					<trim prefixOverrides="OR">
						<choose>
							<when test="cri.type == 'T'.toString()">
								TITLE LIKE '%'||#{cri.keyword}||'%' 
							</when>
							<when test="cri.type == 'C'.toString()">
								CONTENT LIKE '%'||#{cri.keyword}||'%' 
							</when>
							<when test="cri.type == 'W'.toString()">
								WRITER LIKE '%'||#{cri.keyword}||'%' 
							</when>
							<otherwise>
								TITLE LIKE '%'||#{cri.keyword}||'%' OR CONTENT LIKE '%'||#{cri.keyword}||'%' OR WRITER LIKE '%'||#{cri.keyword}||'%'  						
							</otherwise>
						</choose>
					</trim>
				</foreach>
			</trim>
		</if>
	</sql>
	
	<!-- 게시글 목록(검색 포함) -->
	<select id="getListWithPaging" resultType="com.campfire.domain.marketBoard.MarketBoardVO">
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE, REPLYCNT, MARKETKEYWORD 
		FROM
			(SELECT /*+ INDEX_DESC(TBL_MARKET_BOARD PK_MARKET_BOARD)*/
				ROWNUM R, BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE, REPLYCNT, MARKETKEYWORD 
				FROM TBL_MARKET_BOARD 
				WHERE <!-- <include refid="criteria"/> -->
					ROWNUM <![CDATA[<=]]> #{pageNum} * #{amount}
					AND MARKETKEYWORD = #{marketKeyword})
		WHERE R <![CDATA[>]]> (#{pageNum} - 1) * #{amount}
	</select>
	
	<!-- 총 게시글 개수(검색 포함) -->
	<select id="getTotal" resultType="_int">
		SELECT COUNT(BNO) FROM TBL_MARKET_BOARD
		<where>
			<trim suffixOverrides="AND">
				<include refid="criteria"/>
			</trim>
		</where>
	</select>
	
	<insert id="insertBoard">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			SELECT SEQ_MARKET_BOARD.NEXTVAL FROM DUAL 
		</selectKey>
		INSERT INTO TBL_MARKET_BOARD (BNO, TITLE, CONTENT, WRITER, MARKETKEYWORD) 
		VALUES (#{bno}, #{title}, #{content}, #{writer}, #{marketKeyword})
	</insert>
	
	<select id="read" resultType="com.campfire.domain.marketBoard.MarketBoardVO">
		SELECT * FROM TBL_MARKET_BOARD WHERE BNO = #{bno}
	</select>
	
	<select id="selectBoard" resultType="com.campfire.domain.marketBoard.MarketBoardVO">
		SELECT BNO, TITLE, WRITER, REGDATE, UPDATEDATE, REPLYCNT, MARKETKEYWORD FROM TBL_MARKET_BOARD WHERE BNO = #{bno}
	</select>
	
	
	<update id="updateBoard">
		UPDATE TBL_MARKET_BOARD 
		SET TITLE = #{title}, CONTENT = #{content}, UPDATEDATE = SYSDATE 
		WHERE BNO = #{bno}
	</update>
	
	<delete id="deleteBoard">
		DELETE TBL_MARKET_BOARD WHERE BNO = #{bno}
	</delete>
	
	<update id="updateReplyCnt">
		UPDATE TBL_MARKET_BOARD 
		SET REPLYCNT = REPLYCNT + #{amount} 
		WHERE BNO = #{bno}
	</update>
</mapper>