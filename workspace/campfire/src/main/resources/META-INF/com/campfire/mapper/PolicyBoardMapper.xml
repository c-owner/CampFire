<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campfire.mapper.PolicyMapper">
	<!-- 검색 -->
	<sql id="keyword">
		<choose>
			<when test="keyword != null and keyword != ''">
				TITLE LIKE '%'||#{keyword}||'%' 
				OR CONTENT LIKE '%'||#{keyword}||'%' 
				OR WRITER LIKE '%'||#{keyword}||'%' 
				OR ADDRESS LIKE '%'||#{keyword}||'%' 
				OR ADDRESS_DETAIL LIKE '%'||#{keyword}||'%' 
				OR ADDRESS_ETC LIKE '%'||#{keyword}||'%' 
				OR CAMPNAME LIKE '%'||#{keyword}||'%' 
				AND
			</when>
		</choose>
	</sql>
	
	<!-- 게시글 목록(검색 포함) -->
	<select id="getListWithPaging" resultType="com.campfire.domain.PolicyVO">
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE, POLICYKEYWORD
		FROM
			(SELECT /*+ INDEX_DESC(TBL_POLICY_BOARD PK_MARKET_BOARD)*/
				ROWNUM R, BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE, POLICYKEYWORD  
				FROM TBL_POLICY_BOARD 
				WHERE <include refid="keyword"/>
					ROWNUM <![CDATA[<=]]> #{pageNum} * #{amount}
					AND POLICYKEYWORD = #{policyKeyword})
		WHERE R <![CDATA[>]]> (#{pageNum} - 1) * #{amount}
	</select>
	
	<!-- 총 게시글 개수(검색 포함) -->
	<select id="getTotal" resultType="_int">
		SELECT COUNT(BNO) FROM TBL_POLICY_BOARD
		<where>
			<trim suffixOverrides="AND">
				<include refid="keyword"/>
			</trim>
		</where>
	</select>
	
	<select id="categoryTotal" resultType="_int">
		SELECT COUNT(BNO) FROM TBL_POLICY_BOARD WHERE
		<if test="type != null and keyword != null">
			<include refid="keyword"/>
		</if>
		POLICYKEYWORD = #{policyKeyword}
	</select>
	
	<insert id="insertBoard">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			SELECT SEQ_POLICY_BOARD.NEXTVAL FROM DUAL 
		</selectKey>
		INSERT INTO TBL_POLICY_BOARD (BNO, TITLE, CONTENT, WRITER, POLICYKEYWORD) 
		VALUES (#{bno}, #{title}, #{content}, #{writer}, #{policyKeyword})
	</insert>
	
	<select id="read" resultType="com.campfire.domain.PolicyVO">
		SELECT * FROM TBL_POLICY_BOARD WHERE BNO = #{bno}
	</select>
	
	<select id="selectBoard" resultType="com.campfire.domain.PolicyVO">
		SELECT BNO, TITLE, WRITER, REGDATE, UPDATEDATE, POLICYKEYWORD FROM TBL_POLICY_BOARD WHERE BNO = #{bno}
	</select>
	
	
	<update id="updateBoard">
		UPDATE TBL_POLICY_BOARD 
		SET POLICYKEYWORD = #{policyKeyword}, TITLE = #{title}, CONTENT = #{content}, UPDATEDATE = SYSDATE 
		WHERE BNO = #{bno}
	</update>
	
	<delete id="deleteBoard">
		DELETE TBL_POLICY_BOARD WHERE BNO = #{bno}
	</delete>
	
</mapper>